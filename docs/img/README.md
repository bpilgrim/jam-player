# Demo assets

Drop product visuals here.

**Highest priority:** a short GIF of the core loop — **import → tag → filter → play**.
A visual product is judged in seconds; a demo GIF at the top of the main README
does more than the entire write-up.

Suggested files:
- `browse.png` — the browse grid / main window
- `demo.gif` — the import → tag → filter → play loop

Once added, uncomment the image reference near the top of the root `README.md`.
